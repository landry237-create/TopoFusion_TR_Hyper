[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "topofusion-tr-hyper"
version = "0.2.0"
description = "Topology-guided multimodal representation learning with PyTorch"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
